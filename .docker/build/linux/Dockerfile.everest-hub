FROM projecteverest/everest-linux

RUN rm -rf everparse mitls-fstar
ADD --chown=test:test . everparse

ARG CI_THREADS=16
ENV FSTAR_HOME="$HOME/FStar"
ENV KRML_HOME="$HOME/karamel"
ENV HACL_HOME="$HOME/hacl-star"
ENV EVERPARSE_HOME="$HOME/everparse"

RUN make -C everparse -j $CI_THREADS ci
